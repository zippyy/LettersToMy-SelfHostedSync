# One key per line: device_name:token
# Generate a strong token with: openssl rand -hex 32
# The server intentionally refuses to start until this file has an active key.
# Example (replace the token before starting):
# iphone:replace-with-a-random-token
